# Rust / cargo build artifacts
/target
debug/
**/*.rs.bk

# Editor temporary files
*~
.\#*
\#*\#

# macOS
.DS_Store

# Preserved esp-idf cmake 3.30.2 toolchain (240 MB; build.sh points here — not committed)
/tools/

# Build / run logs
/build*.log
/build.log
/run.out

# Serial-monitor / tool captures — runtime logs; chip-tool.md/.out also carry the
# Active Operational Dataset incl. the Thread network key. NEVER commit these.
/esp*.out
/chip-tool.out
/chip-tool.md

# Claude Code session pointer (local)
/claude-latest-session
